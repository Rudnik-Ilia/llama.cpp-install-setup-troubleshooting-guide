GGML_CUDA=ONEnables NVIDIA CUDA acceleration.Compiles kernels specifically for NVIDIA GPUs to offload tensor computations.GGML_METAL=ONEnables Apple Silicon Metal acceleration.Utilizes the unified memory and GPU on Mac M1/M2/M3/M4 chips (enabled by default on macOS).GGML_VULKAN=ONEnables Vulkan graphics API support.Provides cross-platform GPU acceleration for AMD, Intel, and NVIDIA graphics cards.GGML_SYCL=ONEnables Intel SYCL acceleration.Targets Intel Arc GPUs and data center Max Series hardware.GGML_OPENBLAS=ONEnables OpenBLAS library support.Maximizes standard CPU performance through parallelized matrix algorithms.💻 CPU Instruction OptimizationsThese flags instruct the compiler to use specific instruction sets native to your processor architecture.GGML_NATIVE=ONForces the compiler to target your exact local CPU architecture.Autodetects and enables all hardware extensions available on your current system (e.g., AVX2, AVX512).GGML_AVX2=ON / GGML_AVX512=ONExplicitly toggles Advanced Vector Extensions 2 or 512 for Intel/AMD CPUs.Dramatically improves token processing speeds on systems that lack native target detection.GGML_FMA=ONEnables Fused Multiply-Add instruction sets.Combines addition and multiplication into a single step for faster floating-point math.⚙️ Feature & Packaging AdjustmentsThese parameters modify how the overall project binaries are generated and linked.BUILD_SHARED_LIBS=ONCompiles llama.cpp components as dynamic/shared libraries (.so, .dll, .dylib).Turning this OFF packages everything statically into a single independent binary.GGML_BACKEND_DL=ONActivates dynamic loading for hardware backends.Allows a single compiled binary to dynamically load its CUDA or Vulkan modules at runtime depending on what hardware is present.GGML_LLAMAFILE=ONIntegrates localized matrix multiplication kernels originally optimized for the llamafile project.Boosts CPU prompt-processing speeds for smaller prompt sequences under 1,000 tokens.CMAKE_BUILD_TYPE=Release / DebugRelease: Strips debug code and optimizes the application for maximum inference speed.Debug: Leaves the code unoptimized and includes system symbols to help developers troubleshoot bugs.
